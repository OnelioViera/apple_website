import { appleImg } from '../utils'

const Navbar = () => {
  return (
    <header>
      <nav className="">
        <img
          src={appleImg}
        />
      </nav>
    </header>
  )
}

export default Navbar